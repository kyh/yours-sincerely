import styled from 'styled-components';

const PageFooter = styled.div`
  padding: 1rem;
  text-align: center;
  opacity: 0.3;
  letter-spacing: -1px;
  font-size: 0.8rem;
`;

export default PageFooter;
