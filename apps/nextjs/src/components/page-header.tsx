export const PageHeader = ({ title }: { title: string }) => (
  <header className="area-content-header flex items-center border-b border-b-gray-400">
    <h1 className="text-2xl font-bold tracking-tight">{title}</h1>
  </header>
);
